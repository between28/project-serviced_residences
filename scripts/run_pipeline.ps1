# Pipeline entry-point template.
# Replace placeholder commands with project scripts as they are implemented.

Write-Host "Pipeline template: implement stage commands in scripts/ and src/."
Write-Host "Suggested order: ingest -> clean -> linkage -> feature_engineering -> modeling -> outputs."
